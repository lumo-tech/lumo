import torch
#
# t = torch.cuda.get_device_properties(0).total_memory
# c = torch.cuda.memory_cached(0)
# a = torch.cuda.memory_stats(2)
# torch.cuda.get_device_properties(2)
# torch.__version__
#
# import subprocess
